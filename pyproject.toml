[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "example_package_keshav"
version = "0.1.0"
dependencies = ["numpy"]
readme = "README.md"

[tool.hatch.build.targets.wheel]
packages = ["src/example_package_keshav"]

[project.optional-dependencies]
test = ["pytest"]